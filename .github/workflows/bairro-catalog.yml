# GirlMath — Bairro da Saúde daily price refresh
# ============================================================
# Bairro da Saúde é Shopify → /products.json devolve JSON limpo.
# Scrape inteiro corre em ~30 segundos sem browser automation.
# Por isso fazemos diariamente (preços de farmácia mudam todos os dias).
#
# Pipeline:
#   1. scrape-bairro-saude-catalog.js  → data/catalog/bairro-saude-full.json
#   2. integrate-bairro-saude-catalog.js → merge no seed (fuzzy + dedup-audit)
#   3. inject-seed-into-demo.js → regenera demo.html / index.html / catalogo.html
#   4. commit + push para main (Netlify redeploya automaticamente)

name: Bairro da Saúde daily refresh

on:
  schedule:
    # 05h UTC — depois de Easyfarma (04h), antes de Druni (06h)
    - cron: '0 5 * * *'
  workflow_dispatch:
    inputs:
      limit:
        description: 'Limit (vazio = sem limite, útil para teste)'
        required: false
        default: ''

permissions:
  contents: write   # precisamos de fazer push do seed + HTML actualizados


jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install deps
        run: |
          if [ -f package-lock.json ]; then
            npm ci --no-audit --no-fund
          else
            npm install --no-audit --no-fund
          fi

      - name: Scrape Bairro da Saúde catalog
        run: |
          if [ -n "${{ github.event.inputs.limit }}" ]; then
            node scripts/scrape-bairro-catalog.js --limit=${{ github.event.inputs.limit }}
          else
            node scripts/scrape-bairro-catalog.js
          fi

      - name: Upload scrape artifact (safety-net)
        # Garante que mesmo se o integrate falhar / push der conflict,
        # temos os dados scraped guardados como artifact descarregável.
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bairro-saude-catalog-${{ github.run_id }}
          path: data/catalog/bairro-saude-full.json
          retention-days: 14

      - name: Integrate into seed
        run: node scripts/integrate-bairro-catalog.js

      - name: Commit changes
        env:
          GIT_AUTHOR_NAME: GirlMath Bot
          GIT_AUTHOR_EMAIL: bot@girlmath.pt
          GIT_COMMITTER_NAME: GirlMath Bot
          GIT_COMMITTER_EMAIL: bot@girlmath.pt
        run: |
          set -e
          if git diff --quiet -- data/seed-bundle.json demo.html index.html catalogo.html; then
            echo "Nenhuma alteração — nada a fazer."
            exit 0
          fi
          # -f para forçar o bairro-saude-full.json que está gitignored
          git add data/seed-bundle.json demo.html index.html catalogo.html
          git add -f data/catalog/bairro-saude-full.json 2>/dev/null || true
          DATE=$(date -u +%Y-%m-%d)
          git commit -m "chore: Bairro da Saúde daily refresh ($DATE)" \
            -m "Auto-update via GitHub Actions. Sem mexer noutras lojas."

      - name: Push to main (resilient — re-integra sobre origin em conflito)
        run: |
          set -e
          bash scripts/ci/resilient-push.sh \
            "node scripts/integrate-bairro-catalog.js" \
            "chore: Bairro da Saúde daily refresh ($(date -u +%Y-%m-%d))" \
            data/catalog/bairro-saude-full.json
