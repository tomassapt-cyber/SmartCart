# CosMath — Dermis daily price refresh
# ============================================================
# Nuxt SSR; vm-eval do __NUXT__ (sku=CNP; payload posicional). ~3.6k fichas.
#
# Pipeline:
#   1. scrape-dermis-catalog.js → data/catalog/dermis-full.json
#   2. integrate-dermis-catalog.js (match EAN/CNP + pós-processo + inject)

name: Dermis daily refresh

on:
  schedule:
    - cron: '5 14 * * *'
    - cron: '5 22 * * *'   # 3x/dia (regra do user 2026-07-03)
    - cron: '5 6 * * *'   # 3x/dia
  workflow_dispatch:
    inputs:
      limit:
        description: 'Limit (vazio = sem limite)'
        required: false
        default: ''
      resume:
        description: 'Retomar do catálogo committed (1 = sim; vazio = scrape completo)'
        required: false
        default: ''

permissions:
  contents: write
jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
      NODE_OPTIONS: '--max-old-space-size=4096'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install deps
        run: |
          if [ -f package-lock.json ]; then
            npm ci --no-audit --no-fund
          else
            npm install --no-audit --no-fund
          fi

      - name: Scrape Dermis
        run: |
          RESUME=""
          if [ -n "${{ github.event.inputs.resume }}" ]; then RESUME="--resume"; fi
          if [ -n "${{ github.event.inputs.limit }}" ]; then
            node scripts/scrape-dermis-catalog.js --limit=${{ github.event.inputs.limit }}
          else
            node scripts/scrape-dermis-catalog.js
          fi

      - name: Upload scrape artifact (safety-net)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dermis-catalog-${{ github.run_id }}
          path: data/catalog/dermis-full.json
          retention-days: 14

      - name: Integrate into seed
        run: node scripts/integrate-dermis-catalog.js

      - name: Commit changes
        env:
          GIT_AUTHOR_NAME: CosMath Bot
          GIT_AUTHOR_EMAIL: bot@cosmath.pt
          GIT_COMMITTER_NAME: CosMath Bot
          GIT_COMMITTER_EMAIL: bot@cosmath.pt
        run: |
          set -e
          if git diff --quiet -- data/seed-bundle.json demo.html; then
            echo "Nenhuma alteração — nada a fazer."
            exit 0
          fi
          git add data/seed-bundle.json demo.html
          git add -f data/catalog/dermis-full.json 2>/dev/null || true
          DATE=$(date -u +%Y-%m-%d)
          git commit -m "chore: Dermis daily refresh ($DATE)" \
            -m "Auto-update via GitHub Actions. Sem mexer noutras lojas."

      - name: Push to main (resilient — re-integra sobre origin em conflito)
        run: |
          set -e
          bash scripts/ci/resilient-push.sh \
            "node scripts/integrate-dermis-catalog.js" \
            "chore: Dermis daily refresh ($(date -u +%Y-%m-%d))" \
            data/catalog/dermis-full.json
