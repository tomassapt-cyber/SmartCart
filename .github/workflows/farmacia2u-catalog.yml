# CosMath — Farmacia2U daily price refresh
# ============================================================
# farmacia2u.com Fidelizarte: JSON-LD sku=CNP + gtin13; sitemap-cemiterio (~2/3 mortos, HTTP 200 com texto de produto inexistente).
#
# Pipeline:
#   1. scrape-farmacia2u-catalog.js → data/catalog/farmacia2u-full.json
#   2. integrate-farmacia2u-catalog.js (match EAN/CNP + pós-processo + inject)

name: Farmacia2U daily refresh

on:
  schedule:
    - cron: '45 11 * * *'
    - cron: '45 19 * * *'   # 3x/dia (regra do user 2026-07-03)
    - cron: '45 3 * * *'   # 3x/dia
  workflow_dispatch:
    inputs:
      limit:
        description: 'Limit (vazio = sem limite)'
        required: false
        default: ''
      resume:
        description: 'Retomar do catálogo committed (1 = sim; vazio = scrape completo)'
        required: false
        default: ''

permissions:
  contents: write
jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
      NODE_OPTIONS: '--max-old-space-size=4096'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install deps
        run: |
          if [ -f package-lock.json ]; then
            npm ci --no-audit --no-fund
          else
            npm install --no-audit --no-fund
          fi

      - name: Scrape Farmacia2U
        run: |
          RESUME=""
          if [ -n "${{ github.event.inputs.resume }}" ]; then RESUME="--resume"; fi
          if [ -n "${{ github.event.inputs.limit }}" ]; then
            node scripts/scrape-farmacia2u-catalog.js --limit=${{ github.event.inputs.limit }} --concurrency=4 --delay=300 $RESUME
          else
            node scripts/scrape-farmacia2u-catalog.js --concurrency=4 --delay=300 $RESUME
          fi

      - name: Upload scrape artifact (safety-net)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: farmacia2u-catalog-${{ github.run_id }}
          path: data/catalog/farmacia2u-full.json
          retention-days: 14

      - name: Integrate into seed
        run: node scripts/integrate-farmacia2u-catalog.js

      - name: Commit changes
        env:
          GIT_AUTHOR_NAME: CosMath Bot
          GIT_AUTHOR_EMAIL: bot@cosmath.pt
          GIT_COMMITTER_NAME: CosMath Bot
          GIT_COMMITTER_EMAIL: bot@cosmath.pt
        run: |
          set -e
          if git diff --quiet -- data/seed-bundle.json demo.html index.html catalogo.html; then
            echo "Nenhuma alteração — nada a fazer."
            exit 0
          fi
          git add data/seed-bundle.json demo.html index.html catalogo.html
          git add -f data/catalog/farmacia2u-full.json 2>/dev/null || true
          DATE=$(date -u +%Y-%m-%d)
          git commit -m "chore: Farmacia2U daily refresh ($DATE)" \
            -m "Auto-update via GitHub Actions. Sem mexer noutras lojas."

      - name: Push to main (resilient — re-integra sobre origin em conflito)
        run: |
          set -e
          bash scripts/ci/resilient-push.sh \
            "node scripts/integrate-farmacia2u-catalog.js" \
            "chore: Farmacia2U daily refresh ($(date -u +%Y-%m-%d))" \
            data/catalog/farmacia2u-full.json
